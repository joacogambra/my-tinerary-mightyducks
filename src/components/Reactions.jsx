import React from "react"
import { useState } from "react"
import { useEffect } from "react"
import { useDispatch, useSelector } from "react-redux"
import actionForReaction from "../redux/actions/actionForReaction"
import ReactionsLike from "./ReactionsLike"

export default function Reaction(props) {
  


  return (
    <div className="uniqueReaction">
      {/* {uniqueReaction.map((i) => (<ReactionsLike reaction={i} key={i._id} itineraryId={itinerary}/>))} */}
    </div>
  )
}


